# nnU-Net raw data

Placeholder directory for the `nnUNet_raw` dataset folder (`DatasetXXX_.../imagesTr`,
`labelsTr`, `dataset.json`).

Contents are **not** tracked in git and are regenerated by the dataset-conversion
script in `../nnUNet_scripts`.
