# nnU-Net preprocessed data

Placeholder directory for nnU-Net's own preprocessing cache (the `nnUNet_preprocessed`
environment variable points here).

Contents are **not** tracked in git and are regenerated by `nnUNetv2_plan_and_preprocess`.
